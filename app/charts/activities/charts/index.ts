import DistancePerSeason from './DistancePerSeason';
import HeartrateProgression from './HeartrateProgression';
import TrainingLoadPerWeek from './TrainingLoadPerWeek';

const CHARTS = {
  'distance-per-season': DistancePerSeason,
  'heartrate-progression': HeartrateProgression,
  'training-load-per-week': TrainingLoadPerWeek,
};

export default CHARTS;
